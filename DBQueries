DataBase Tables

users
CREATE TABLE users (
    username VARCHAR(50) NOT NULL,
    email VARCHAR(100) NOT NULL PRIMARY KEY,
    password VARCHAR(255) NOT NULL
);


Results
CREATE TABLE results (
    result_id INT AUTO_INCREMENT PRIMARY KEY,
    user_email VARCHAR(100),
    reference_text TEXT NOT NULL,
    audio_file MEDIUMBLOB, -- Assuming MEDIUMBLOB for .wav files, adjust based on your file sizes
    overall_score INT NOT NULL,
    result_date DATE NOT NULL
);


User analytics will be available for only latest 5 days
SELECT overall_score
FROM results
WHERE result_date >= CURDATE() - INTERVAL 5 DAY
ORDER BY result_date DESC;


User history for last 20 records will be displayed
SELECT *FROM results
WHERE result_date >= CURDATE() - INTERVAL 20 DAY
ORDER BY result_date DESC;
